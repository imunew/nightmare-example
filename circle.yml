machine:
  node:
    version: 0.10.33
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    # application
    - npm install
    - cp tests/config.coffee.circle.dist tests/config.coffee
  post:

test:
  override:
    - npm run e2e-test:example
