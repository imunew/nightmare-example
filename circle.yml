machine:
  node:
    version: 0.10.33
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - cp tests/config.coffee.circleci.dist tests/config.coffee
  post:

test:
  override:
    - npm run e2e-test:example
  post:
    - cp -r logs/screenshots $CIRCLE_ARTIFACTS
